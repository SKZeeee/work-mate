# 技術スタック選定メモ

## 前提

要件と追加回答から、アプリは次の性質を持つと解釈する。

- `Windows` と `macOS` の両方で動作する
- 会社のローカル PC でも開発しやすいことが重要
- `Node.js` は導入できるが、`Rust` は導入できない前提
- 32x32 のドット絵キャラクターを常時表示する
- 透過背景の小さなウィンドウとして画面右下に常駐する
- 最前面表示にする
- 初期版ではドット絵は 2 フレームのみでよい
- 初期版では「ふわふわ浮く」アニメーションだけを実装する
- 初期版では `ログイン時自動起動` は不要
- 初期版では `クリック/ドラッグなどのインタラクション` は不要

## 結論（確定）

最初の実装は `Electron + TypeScript + Canvas 2D` で進める。

## 推奨技術スタック

### アプリ本体

- デスクトップ基盤: `Electron`
- メインプロセス言語: `TypeScript`
- レンダラー言語: `TypeScript`
- 描画: `Canvas 2D`
- ビルド: `TypeScript Compiler (tsc)`

### アセット管理

- 初期版: コード上で 2 フレームのドット絵を直接定義
- 将来: `Aseprite` で作成した `PNG スプライトシート + JSON`

### データ保存

- 初期版: 永続化なし

### 開発・ビルド

- Node.js ランタイム: `最新の LTS 系` を推奨
- パッケージ管理: `npm`
- テスト: 初期版はビルド確認と目視確認を優先

### 配布

- Windows: `electron-builder` などで `NSIS` または `portable exe`
- macOS: `.app` / `DMG`

## この構成を推す理由

### 1. Rust 不要で開発できる

`Tauri` はビルド時に `Rust` が必須だが、今回の前提では会社 PC への導入が難しい。
`Electron` なら `Node.js` だけで開発を進められる。

### 2. 必要なウィンドウ制御を素直に扱える

今回必要なのは主に次の 4 点である。

- 透過ウィンドウ
- 枠なしウィンドウ
- 最前面表示
- 画面右下への固定配置

`Electron` なら `BrowserWindow` と `screen` API でこの要件をそのまま実装できる。

### 3. 最小実装を作りやすい

初期版は設定画面も複雑な UI も不要で、2 フレームのドット絵をループ再生できればよい。
この条件では `Canvas 2D` を直接描くだけで十分であり、追加の UI ライブラリも不要である。

## 比較した候補

### 採用: Electron + TypeScript + Canvas 2D

向いているケース:

- Node.js は使える
- Rust は避けたい
- クロスプラットフォームで早く動くものを作りたい

懸念:

- `Tauri` よりメモリ使用量は多い
- 配布サイズも比較的大きい

### 不採用: Tauri 2 + TypeScript + Canvas 2D

不採用理由:

- 開発・ビルドに `Rust` が必要
- 今回の会社 PC 制約と合わない

### 条件付き候補: Python + PySide6

向いているケース:

- Python に統一したい
- 配布より社内運用を優先する

懸念:

- 将来の配布構成がやや煩雑になりやすい
- 今回は Electron の方が要件との対応が単純

## 実装方針のたたき台

### ウィンドウ

- 透明背景
- タイトルバーなし
- リサイズ不可
- 最前面表示
- 起動時に画面右下へ移動
- 初期版ではクリック透過を有効化

### 右下配置の方法

- `screen.getPrimaryDisplay().workArea` を使う
- 作業領域サイズからウィンドウサイズを引いて右下座標を計算する
- タスクバーや Dock と重ならない余白を持たせる

### アニメーション

- 2 フレームのドット絵を交互に切り替える
- `requestAnimationFrame` で上下にゆるく揺らす
- 将来ランダムアニメーションを追加できる構造にする

### 表示サイズ

- 元画像は `32x32`
- 実表示は `160x160` ウィンドウ内で `4x` 拡大表示
- 補間を無効にしてドット感を維持する

## 現時点の推奨

初期版は `Electron + TypeScript + Canvas 2D` で確定とする。
依存は `electron`, `typescript`, `@types/node` の最小限に抑える。

## 最小構成の提案

### Electron 側

- `electron/main.ts`: ウィンドウ生成、最前面、右下固定

### レンダラー側

- `src/renderer.ts`: Canvas 初期化、2 フレーム描画、ふわふわアニメーション
- `index.html`: 透明ウィンドウ用の最小 HTML
- `styles.css`: 透明背景と canvas の基本スタイル

### 設定

- `package.json`: スクリプトと依存定義
- `tsconfig.electron.json`: Electron メインプロセス用設定
- `tsconfig.renderer.json`: レンダラー用設定

## 初期実装の推奨値

- 表示サイズ: `160x160`
- 配置先: `メインディスプレイの右下`
- 右下余白: `16px`
